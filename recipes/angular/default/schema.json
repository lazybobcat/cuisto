{
    "name": "angular",
    "main": "index.js",
    "properties": {
        "ngListenPort": {
            "type": "number",
            "description": "What port should the Angular server listen on?",
            "default": 4200
        }
    }
}
